{% extends 'base.html.twig' %}

{% block title %}New Media{% endblock %}

{% block body %}
    <h1>Create new Media</h1>
        <div class="container p-3">
            <div class="panel ">
                <div class="panel-heading ">
                    <h1>Nouveau Media</h1>
                </div>
                <div class=" d-flex justify-content-center align-items-center">
                    <div class="col-sm-6 panel-body">
                        {{ form_start(form) }}
                        <div class="form-group">
                            {{ form_row(form.Nom) }}
                        </div>
                        <div class="form-group">
                            {% if medium.photo %}
                                <img src="{{ asset('photo/'~ medium.photo)}}" alt="" width="100" height="50"> {{ medium.photo }}</td>
                                <input type="hidden" name="old_image" id="old_image" value="{{  medium.photo }}">
                            {% endif %}
                            {{ form_row(form.photo) }}
                        </div>
                        {{ form_widget(form) }}
                        <div class="d-flex justify-content-between">
                            <a class="btn btn-outline-secondary" href=" {{path('media_index') }}">Retour</a>
                            <input class="btn btn-outline-success" type="submit" value="Enregister" />
                        </div>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
{% endblock %}
